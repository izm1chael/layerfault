[package]
name = "layerfault-telemetry-protocol"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
publish = false
description = "Shared wire-protocol types for Layerfault's optional eBPF telemetry helper"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
